#include <stdio.h>
//Error lvalue required post fix operator cannot be used with the name of the array
/*
int main(){
    int i;
    char *a[] = "Hello";
    while(a != '\0'){
        printf("%c", a);
        *a = *a + 1;
    }
    return 0;
}
*/