#include <stdio.h>

int main(){
    float a;
    int x = 6;
    int y = 4;

    //a = x / y;
    a = (float)x / y;
    //printf("Value of a = %f\n", a);
    printf("Value of a = %f\n", a);
    return 0;
}