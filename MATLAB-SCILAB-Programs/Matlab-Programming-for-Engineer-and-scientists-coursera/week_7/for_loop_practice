function summa = halfsum(A)
total = 0
for i =1:size(A,1)
    for j = size(A,2) : -1:i
        total = total +A(i,j)
    end
end
    summa = total;

%code to call function
summa = halfsum([1 2 3; 4 5 6; 7 8 9])
